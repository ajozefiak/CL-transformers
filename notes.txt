squeue -p sched_mit_sloan_gpu_r8 -t R   # Running jobs
squeue -p sched_mit_sloan_batch_r8 -t R   # Queued jobs
noglob scp jozefiak@eofe8.mit.edu:/nobackup1/jozefiak/CL/Results/ATN/*.png .

ssh jozefiak@eofe8.mit.edu

# Check the memory of the current directory
du -sh .

# Delete any file or file in a subdirectory 
find . -name "reset_mask_array.pkl" -type f -delete

# Check the runtime of jobs in the last 7 days
sacct --user=$USER --format=JobID,JobName,State,Elapsed --starttime=$(date -d '7 days ago' +%Y-%m-%d)

# Check priorities of users with pending jobs
squeue -p sched_mit_sloan_gpu_r8 -t PD -o "%.8u %.10Q" | tail -n +2 | sort | awk '{arr[$1]+=$2; count[$1]++} END {for (u in arr) printf "%-8s Total_Priority: %-10d Pending_Jobs: %d\n", u, arr[u], count[u]}'

ssh jozefiak@eofe8.mit.edu 

Sort pending jobs based on priority and list priority value:
squeue -p sched_mit_sloan_gpu_r8 -t PD -o "%.18i %.9P %.8j %.8u %.2t %.10M %.6D %.10R %.10Q" --sort=-Q

Check how long a job ran on the cluster:
sacct -j  62543181_0 --format=JobID,JobName,Elapsed

Specifiy a start time: 
sbatch --begin=now+2hours file_name.sh 3

